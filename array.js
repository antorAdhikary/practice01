var friendAge =[44, 11, 55 ]
console.log(friendAge[1]);
var friendName = ['selim', 'korim', 'jakkas', 'selim', 'korim', 'jakkas' ]

var i = 0;
while (i < friendName.length){
    console.log(friendName[i])
    i++;
}

for (let i = 0; i < friendAge.length; i++) {
    const element = friendAge[i];
    console.log(element);
}